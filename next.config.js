/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'images.pexels.com'],
    // Uncomment if you're using remote images from other domains
  },
}

module.exports = nextConfig
